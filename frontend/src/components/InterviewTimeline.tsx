import { ClockCircleOutlined } from '@ant-design/icons';
import { Timeline, Tag, Space } from 'antd';
import dayjs from 'dayjs';
import { statusText } from '../constants/enums';
export default function InterviewTimeline({ interviews }: { interviews: Interview[] }) { return <Timeline items={interviews.map((i)=>({dot:<ClockCircleOutlined/>, children:<div><b>第 {i.round} 轮 · {i.interviewer?.name || '待定面试官'}</b><div style={{marginTop:4,marginBottom:4}}>{dayjs(i.scheduledAt).format('YYYY-MM-DD HH:mm')} · {i.duration} 分钟 · <Tag>{statusText[i.result]}</Tag>{i.score ? <Tag color="gold">总分 {i.score}/10</Tag> : null}</div>{(i.professionalScore !== undefined || i.communicationScore !== undefined || i.cultureScore !== undefined) && <Space size={8} style={{marginBottom:8,fontSize:12}}>{i.professionalScore !== undefined && <Tag color="blue">专业能力 {i.professionalScore}</Tag>}{i.communicationScore !== undefined && <Tag color="cyan">沟通协作 {i.communicationScore}</Tag>}{i.cultureScore !== undefined && <Tag color="purple">文化匹配 {i.cultureScore}</Tag>}</Space>}<p>{i.feedback || i.notes || '等待面试反馈'}</p></div>}))}/>; }
